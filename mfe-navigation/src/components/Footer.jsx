import React from 'react';

const footerStyle = {
    border: "3px dotted limegreen",
    borderRadius: 10,
    padding: 20
}

const Footer = () => {
    return (
        <div style={footerStyle}>
            I am footer
        </div>
    );
};

export default Footer;
import React from 'react';

const imageStyle = {
    backgroundColor: "#FFD580",
    height: 200,
    width: 200
}

const Image = () => {
    return (
        <div style={imageStyle} />
    );
};

export default Image;
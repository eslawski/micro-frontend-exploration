import React from 'react';

const imageStyle = {
    border: "3px dotted gold",
    borderRadius: 10,
    height: 200,
    width: 200
}

const Image = () => {
    return (
        <div style={imageStyle} />
    );
};

export default Image;